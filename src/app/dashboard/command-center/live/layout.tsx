/** Full-viewport live wall — no extra dashboard chrome beyond root layout. */
export default function LiveCommandCenterLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
